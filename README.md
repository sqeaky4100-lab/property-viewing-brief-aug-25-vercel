# Property Viewing Brief — August 25

Plain Next.js App Router + shadcn-style components, packaged for Vercel.

```bash
npm ci
npm run build
npm start
```

No Cloudflare, vinext, Sites runtime, database, or worker files are included.
